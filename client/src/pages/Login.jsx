import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ArrowRight, BriefcaseBusiness, CalendarDays, LockKeyhole, Mail, Sparkles, Sprout } from "lucide-react";
import { loginUser } from "../services/authService";
import useAuth from "../hooks/useAuth";
function Login() { const navigate = useNavigate(); const { login } = useAuth(); const [form, setForm] = useState({ email: "", password: "" }); const handleSubmit = async (e) => { e.preventDefault(); try { const data = await loginUser(form); login(data); navigate("/dashboard"); } catch { alert("Invalid Email or Password"); } }; return <div className="auth-page"><aside className="auth-left"><div className="auth-brand"><Sprout size={21} /> CareerPilot</div><div><p className="eyebrow">YOUR CAREER COMPANION</p><h1>Move your career forward, one opportunity at a time.</h1><p>Track applications, deadlines, and resume feedback in one focused workspace.</p></div><div className="auth-features"><span><BriefcaseBusiness size={18} /> Internship tracking</span><span><CalendarDays size={18} /> Deadline management</span><span><Sparkles size={18} /> AI resume feedback</span></div></aside><main className="auth-right"><section className="auth-card"><p className="eyebrow">WELCOME BACK</p><h2>Sign in to CareerPilot</h2><p className="auth-subtitle">Continue where your career search left off.</p><form onSubmit={handleSubmit}><label>Email<div className="input-wrap"><Mail size={18} /><input type="email" placeholder="you@example.com" onChange={(e) => setForm({ ...form, email: e.target.value })} /></div></label><label>Password<div className="input-wrap"><LockKeyhole size={18} /><input type="password" placeholder="Your password" onChange={(e) => setForm({ ...form, password: e.target.value })} /></div></label><button className="auth-btn">Sign in <ArrowRight size={17} /></button></form><p className="auth-footer">Don't have an account? <Link to="/register">Create one</Link></p></section></main></div>; }
export default Login;
