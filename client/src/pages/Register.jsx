import { useState } from "react";
import { Link, useNavigate } from "react-router-dom";
import { ArrowRight, BriefcaseBusiness, CalendarDays, LockKeyhole, Mail, Sparkles, Sprout, UserRound } from "lucide-react";
import { registerUser } from "../services/authService";
function Register() { const navigate = useNavigate(); const [form, setForm] = useState({ name: "", email: "", password: "" }); const handleSubmit = async (e) => { e.preventDefault(); try { await registerUser(form); alert("Registration Successful"); navigate("/"); } catch { alert("Registration Failed"); } }; return <div className="auth-page"><aside className="auth-left"><div className="auth-brand"><Sprout size={21} /> CareerPilot</div><div><p className="eyebrow">START YOUR JOURNEY</p><h1>A more intentional way to pursue your next role.</h1><p>Bring applications, deadlines, and career momentum into one place.</p></div><div className="auth-features"><span><BriefcaseBusiness size={18} /> Internship tracking</span><span><CalendarDays size={18} /> Smart dashboard</span><span><Sparkles size={18} /> AI resume feedback</span></div></aside><main className="auth-right"><section className="auth-card"><p className="eyebrow">CREATE ACCOUNT</p><h2>Start with CareerPilot</h2><p className="auth-subtitle">Build a clearer, more confident career search.</p><form onSubmit={handleSubmit}><label>Full name<div className="input-wrap"><UserRound size={18} /><input placeholder="Your name" onChange={(e) => setForm({ ...form, name: e.target.value })} /></div></label><label>Email<div className="input-wrap"><Mail size={18} /><input type="email" placeholder="you@example.com" onChange={(e) => setForm({ ...form, email: e.target.value })} /></div></label><label>Password<div className="input-wrap"><LockKeyhole size={18} /><input type="password" placeholder="Create a password" onChange={(e) => setForm({ ...form, password: e.target.value })} /></div></label><button className="auth-btn">Create account <ArrowRight size={17} /></button></form><p className="auth-footer">Already have an account? <Link to="/">Sign in</Link></p></section></main></div>; }
export default Register;
